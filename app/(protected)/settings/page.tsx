import { auth } from "@/auth";

const Setting = () => {
    // const session = auth()
    return (
        <div>
        <h1>Setting</h1>
        {/* <p>{JSON.stringify(session)}</p> */}
        </div>
    );
}

export default Setting;